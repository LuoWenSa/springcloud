server:
  port: 80

# 开启降级feign.hystrix
feign:
  hystrix:
    enabled: true

#Eureka配置
eureka:
  client:
    register-with-eureka: false #表示不向eureka注册中心注册自己
    service-url:
      defaultZone: http://localhost:7001/eureka/

